[project]
name = "gen-ai-fundamentals"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "groq>=1.0.0",
    "openai>=2.15.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
